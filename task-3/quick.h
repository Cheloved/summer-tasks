/**
 * @file quick.h
 * @brief Заголовочный файл для реализации быстрой сортировки строк.
 *
 * Содержит объявление функции `quick_sort()`, которая выполняет рекурсивную
 * быструю сортировку индексов строк текста, используя сравнение через linecmp().
 *
 * @author Ракитин Илья Алексеевич
 */

#pragma once

#include <stdlib.h>
#include "utils.h"

/**
 * @brief Быстрая сортировка массива индексов строк.
 *
 * Рекурсивно разбивает массив на три части (меньше, равно, больше опорного),
 * сортирует подмассивы и возвращает отсортированный массив индексов строк.
 *
 * @param input Указатель на начало текста (общий буфер с текстом).
 * @param array Входной массив индексов строк.
 * @param length Длина входного массива индексов.
 * @param result Выходной массив отсортированных индексов строк.
 * @return Возвращает 0 при успешном завершении.
 */
int quick_sort(char* input, long* array, long length, long* result);
