/**
 * @file sequence.h
 * @brief Заголовочный файл для поиска максимальной возрастающей подпоследовательности.
 *
 * Содержит объявление функции, которая находит в последовательности
 * самую длинную возрастающую подпоследовательность с наибольшей суммой.
 *
 * @author Ракитин Илья Алексеевич
 */

#pragma once 

#include <stdio.h>

/**
 * @brief Находит самую длинную возрастающую подпоследовательность с наибольшей суммой.
 *
 * Функция анализирует входную последовательность целых чисел и находит
 * подпоследовательность, которая:
 * - строго возрастает,
 * - имеет наибольшую сумму (если есть несколько таких — выбирается самая длинная).
 *
 * Результат записывается в выходной массив `result`, а длина результата — в `result_size`.
 *
 * @param seq Входная последовательность целых чисел.
 * @param size Длина входной последовательности.
 * @param result Выходной массив, в который будет записан результат.
 * @param result_size Указатель на переменную, в которую будет записана длина результата.
 * @return Возвращает 0 при успешном завершении.
 */
int find_max_seq(int* seq, int size, int* result, int* result_size);
